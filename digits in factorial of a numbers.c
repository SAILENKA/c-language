#include<stdio.h>
#include<math.h>
int main()
{
    int res=0,num;
    printf("Enter the number:");
    scanf("%d",&num);
    for(int i=1;i<=n;i++)
    {
    res+=log(i);
    printf("%d",res);
}
